# بروتوكول تدريب Pilot لاختيار عائلة Scheduler

## الهدف والاشتمال

لكل حالة نأخذ optimal_policies من v4. نحول RMS وRMS+Boost إلى عائلة RMS،
وEDF وEDF+Boost إلى عائلة EDF. تدخل الحالة التدريب فقط إذا كانت كل السياسات
المثلى ضمن عائلة واحدة. الهدف هو تلك العائلة. الحالات التي تتعادل عبر العائلتين
تظل مهمة لتقييم النظام لاحقًا، لكنها لا تعطي Target فريدًا لهذا المصنف.

هذا الاشتمال ينتقي حالات يظهر فيها فرق داخل نافذة 400 ms؛ لذلك مقاييس التصنيف
لا تصف جميع الحالات التشغيلية. لا يجوز عرض 75% وكأنها دقة Scheduler على كل
التشغيل. وحدة الفصل Task Set، والصفوف المتجاورة داخل المجموعة مترابطة.

## اختيار النموذج

نستخدم DecisionTreeClassifier ثابت random_state=42 وcriterion=gini. شبكة
Balanced Accuracy تختبر max_depth من 2 إلى 6، وmin_samples_leaf من
{10,20,40,80}، وclass_weight بقيمتي None وbalanced. الاختيار بأعلى متوسط
Balanced Accuracy في StratifiedGroupKFold من خمس طيات على train فقط، ثم
الأقل عمقًا عند التعادل.

النموذج الحساس للتكلفة يستبعد current_mode. وزن كل حالة يساوي:

1 + M * critical_regret_of_wrong_family + total_regret_of_wrong_family

حيث M أكبر بواحد من أكبر total regret في train. تختار الشبكة المعاملات بأقل
متوسط critical regret لكل حالة في Group CV، ثم أقل total regret، ثم البساطة.
هذه صياغة تقريبية لهدف معجمي؛ لا تحول الشجرة نفسها إلى Optimizer مباشر.

## المقاييس

نبلغ Accuracy وBalanced Accuracy ومصفوفة الالتباس على الحالات المؤهلة، ثم
Counterfactual regret. لحساب تكلفة اختيار عائلة نعطيها أفضل نتيجة من base
وboost داخل تلك العائلة. هذا حد متفائل لأنه يفترض Selector داخليًا غير موجود.
بالتالي لا يجوز تفسيره كأداء نشر. نقارن baseline يبقى في عائلة behavior_policy.

في النتيجة الحالية الشجرة أعلى في دقة الفئة وأسوأ في regret من baseline.
الأخطاء ليست متساوية التكلفة، ومتوسط الدقة لا يكفي لقبول النموذج. عدم استخدام
current_mode في splits نتيجة خاصة بهذه البيانات، وليس إثباتًا عامًا لعدم فائدته.

## التصدير والقيود

نحفظ feature order مع pickle، ونصدر نص الشجرة ودالة C بمدخلات float. تم تجميع
دوال C الثلاث ومقارنة قراراتها بقرارات scikit-learn على 272 حالة validation؛
تطابقت 272/272 لكل نموذج. هذا يثبت تطابق المسار العددي على هذه الحالات فقط،
ولا يقيس Flash أو SRAM أو زمن القرار على ATmega32.

لم يستخدم test، ولم ندرب Boost selector، ولم نشغّل Scheduler متعلمًا كسلسلة
قرارات مغلقة الحلقة. قبل ادعاء تحسن يلزم تقييمه على تشغيل كامل مستقل ومقارنته
بالسياسات والقواعد المباشرة على نفس الأحمال، مع المقاييس الإحصائية المحددة.
