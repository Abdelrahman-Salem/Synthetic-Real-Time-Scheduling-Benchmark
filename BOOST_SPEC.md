# تعريف Priority Boost في النسخة الثانية

هذا تصميم تجريبي جديد ومعلن، وليس نقلًا حرفيًا من كود AVR القديم. لا توجد
دعوى أن قيم المعاملات مثالية أو أن السياسة تقدم ضمان hard real-time.

## شرط الاختيار

تُحدد أهمية المهمة مسبقًا باستخدام critical=True؛ الافتراضي False. عند كل
حد زمني 1 ms، بعد فحص التأخير والإطلاق، نحسب:

estimated_remaining = WCET - executed

slack = absolute_deadline - now - estimated_remaining

المهمة مؤهلة إذا كانت حرجة وغير مكتملة، ولم يحل موعدها، وما زالت لها ميزانية
WCET موجبة، و0 <= slack <= slack_threshold، ولها رصيد Boost متبقٍّ على
مستوى الإصدار وعلى مستوى النظام. لا تُستخدم قيمة demand المستقبلية الحقيقية
في تحديد أهلية Boost؛ تستخدم الميزانية المعلومة للمجدول فقط.

إذا كانت slack سالبة، لا تنال المهمة Boost لأن إنقاذ موعدها لم يعد ممكنًا
بحسب الميزانية المقدرة. قد تعمل وفق السياسة الأساسية. إذا تجاوز التنفيذ WCET،
لا يوجد تقدير متبقٍّ موثوق في v2، فتُوقف أهلية Boost لذلك الإصدار.

## الترتيب

تُختار مهمة واحدة من المؤهلة، وتسبق جميع المهام غير المعززة. ترتيب المؤهلة:
الموعد النهائي المطلق ثم وقت الإطلاق ثم اسم المهمة ثم رقم الإصدار.
بدون مؤهلات تطبق RMS أو EDF الأساسية، بما فيها سياسة التعادل الأصلية.
بذلك قد يسبق Boost مهمة غير حرجة ذات موعد أقرب تحت EDF؛ مثال tradeoff يوضح الثمن.

## القيم الافتراضية بالمللي ثانية

| المعامل | القيمة | المعنى |
|---|---:|---|
| slack_threshold | 1 | أقصى هامش يسمح بالتعزيز |
| per_job_budget | 2 | أقصى تنفيذ معزز لنفس الإصدار طوال عمره |
| window | 10 | طول نافذة النظام الثابتة |
| window_budget | 2 | أقصى تنفيذ معزز لجميع المهام في النافذة |

النوافذ [0,10)، [10,20)، وهكذا. يسمح التصميم بإعادة التعزيز عند تجدد رصيد
النافذة إذا بقيت شروط الأهلية ورصيد الإصدار متاحين. رصيد الإصدار لا يتجدد.
هذه ميزانية نافذة ثابتة، وليست rolling window؛ قد تتجاور دفعتان عند حد نافذتين.
حتى إذا اختارت السياسة الأساسية نفس المهمة، يُحسب تنفيذها أثناء تفعيل Boost
ضمن الميزانية. هذا مقياس استخدام الآلية وليس مقياسًا للوقت الإضافي الذي كسبته.

## الإلغاء

تُعاد الأهلية كل tick ولا نخزّن تعديلًا دائمًا لأولوية المهمة. ينتهي التعزيز عند
الإكمال، أو انتهاء ميزانية الإصدار/النافذة، أو فقدان أي شرط أهلية، أو اختيار
مهمة مؤهلة أعلى ترتيبًا، أو إسقاط الإصدار، أو نهاية المحاكاة. إذا زال ضغط
الموعد وفق تعريف slack، لا يستمر Boost لمجرد أن النظام كان مزدحمًا سابقًا.
لا توجد إشارة مستقلة لتعريف overload في v2. السياسات ثابتة طوال التشغيل؛
تبديل الأوضاع أثناء التشغيل سيحتاج اختبارًا مستقلًا في مرحلة القرار الهجين.

سجل الأحداث يحتوي boost_start وboost_end وboost_end_complete
وboost_end_horizon. يحتوي JSON أيضًا boost_intervals لحصر التنفيذ المعزز
بدقة، مع boosted_execution لكل Job. مرات التفعيل ليست نسبة زمن التعزيز.

## حدود العدالة والضمانات

الميزانية تحد استحواذ Boost وحده على المعالج؛ لا تثبت انعدام starvation
للمهام الأخرى تحت حمل زائد أو سياسة RMS الأساسية. لا توجد موارد مشتركة
أو locks في هذا النموذج، وبالتالي لا نحاكي priority inversion الناتج عنها.
لا نحسب تكلفة Boost أو تبديل السياق الفعلية بعد. يجب عرض تأخير المهام الحرجة
وغير الحرجة والإجمالي معًا. تغيير المعاملات لاحقًا يكون على بيانات التحقق،
ولا نضبطها على مجموعة الاختبار النهائي.

## مثال تغيير الإعدادات

```python
from simulator import Task, BoostConfig, simulate
tasks = [Task('A', 5, 2, 5), Task('B', 10, 2, 3, critical=True)]
result = simulate(tasks, 10, 'RMS+Boost', BoostConfig(
    slack_threshold=1, per_job_budget=2, window=10, window_budget=2))
print(result['summary'])
```
